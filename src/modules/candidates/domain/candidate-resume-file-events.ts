export const CANDIDATE_RESUME_FILE_EVENT_TYPES = {
  uploadRequested: 'candidate.resume_file.upload_requested',
  uploaded: 'candidate.resume_file.uploaded',
  rejected: 'candidate.resume_file.rejected',
  accessUrlCreated: 'candidate.resume_file.access_url_created',
} as const;
